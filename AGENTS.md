# AGENTS.md — 工程规约（AI 开发必须遵守）

本仓库由 AI 在云端沙箱中开发，push 到 `main` 分支后会**零人工值守自动部署**到生产服务器。
以下规约保证任何技术栈的代码都能被流水线正确构建和发布，**每次提交代码前必须逐条检查**。

## 必须满足（硬性要求）

1. **可容器化**：仓库根目录必须有 `Dockerfile`，`docker build .` 能成功产出可运行的镜像。
2. **监听地址**：服务必须监听 `0.0.0.0`（不能是 `127.0.0.1`）。
3. **端口**：从环境变量 `PORT` 读取，默认 `8080`（容器内部端口，主机侧不暴露，由共享 Caddy 反代）。
4. **健康检查**：必须提供 `GET /healthz`，服务就绪时返回 HTTP 200。流水线以此判断部署成败。
5. **配置**：所有环境相关配置（数据库地址、密钥等）只走环境变量，**禁止硬编码或提交密钥**。
6. **依赖外部服务**（数据库/缓存等）：在 `docker-compose.yml` 中以 service 形式声明，不依赖宿主机。
7. **禁止占用固定主机端口**：本服务与其他演示任务共存于同一台服务器，任何端口冲突都会导致部署失败。端口一律走 `PORT` 环境变量。

## 禁止事项

- 禁止在启动时执行破坏性数据库迁移（删表、清数据）。schema 变更必须是增量的、可向后兼容的。
- 禁止依赖构建环境之外的系统级软件（一切依赖进 Docker 镜像）。
- 禁止修改 `.github/workflows/` 下的流水线文件，除非任务明确要求改 CI。

## 测试

- 有测试框架就写测试：Node 项目 `npm test`，Python 项目 `pytest`，Go 项目 `go test ./...`。
- 流水线会先跑测试，**测试失败不会部署**。

## 部署机制（了解即可，不要改）

push 到 `main` → GitHub Actions 构建镜像 → 推送到服务器 `/opt/apps/<任务名>/` → 健康检查 → 失败自动回滚到上一版本 → 自动注册子域名 `<任务名>.meegodemo.com` 并上线。
多个演示任务共存于同一台服务器，各自独立容器、独立子域名，由共享 Caddy 统一对外（自动 HTTPS）。
`docker-compose.yml` 里的 `IMAGE_NAME`/`CONTAINER_NAME`/`APP_PORT` 由流水线注入，本地开发可直接 `docker compose up`（取默认值）。
