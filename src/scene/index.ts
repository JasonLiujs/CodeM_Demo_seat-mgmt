/**
 * scene/ 目录占位：3D 场景构建模块（地图、环境、天空盒）。
 *
 * 当前基础场景由 engine/Arena.ts 构建（地板 + 围墙 + 掩体 + 光照）。
 * 后续「场景构建」需求在此目录下实现多张抗战主题地图：
 * - 破旧村庄：房屋、围墙、废墟道具
 * - 山地碉堡：碉堡结构、沙袋、铁丝网
 * - 铁路桥：桥梁结构、铁轨、爆炸物道具
 *
 * 对外接口约定：
 * - 每张地图导出一个 SceneBuilder 类，接收 THREE.Scene，构建场景几何体
 * - 场景切换通过 Game.gameState.setWave / setLevel 触发
 */
export {};
