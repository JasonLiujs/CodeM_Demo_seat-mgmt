/**
 * game/ 目录占位：游戏核心逻辑模块（主循环、状态机、场景切换）。
 *
 * 当前主循环由 src/Game.ts 驱动，后续需求可在 game/ 下扩展
 * 场景管理器、关卡控制器等高阶逻辑。
 *
 * 对外接口约定：
 * - 后续需求通过 re-export 从 Game.ts 引入主类
 * - 场景管理器（SceneManager）负责多场景切换
 */
export {};
